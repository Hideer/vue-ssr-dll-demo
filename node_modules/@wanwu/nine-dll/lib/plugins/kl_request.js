import kl_request from '../kl_request';
export default {
  // 请求库全局挂载
  install: function install(Vue) {
    // function doRequest(url, data, ...rest) {
    //     return new Promise((resolve, reject) => {
    //         kl_request(url, data, ...rest)
    //             .then(data => {
    //                 resolve(data);
    //             })
    //             .catch(err => reject(err));
    //     });
    // }
    Vue.prototype.$kl_request = kl_request;
    Vue.kl_request = kl_request;
  }
};