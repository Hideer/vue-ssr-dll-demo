/**
 *file: outerStyle.js
 *@Author: wxm
 *@Date: 2020-04-27 17:20
 **/
export default (function (style) {
  if (!style) return {};
  var obj = {};

  if (typeof style.marginTop === 'number') {
    obj.marginTop = style.marginTop / window.remRadio + 'rem';
  }

  if (typeof style.marginBottom === 'number') {
    obj.marginBottom = style.marginBottom / window.remRadio + 'rem';
  }

  if (typeof style.marginLeft === 'number') {
    obj.marginLeft = style.marginLeft / window.remRadio + 'rem';
  }

  if (typeof style.marginRight === 'number') {
    obj.marginRight = style.marginRight / window.remRadio + 'rem';
  }

  if (typeof style.paddingTop === 'number') {
    obj.paddingTop = style.paddingTop / window.remRadio + 'rem';
  }

  if (typeof style.paddingBottom === 'number') {
    obj.paddingBottom = style.paddingBottom / window.remRadio + 'rem';
  }

  if (typeof style.paddingLeft === 'number') {
    obj.paddingLeft = style.paddingLeft / window.remRadio + 'rem';
  }

  if (typeof style.paddingRight === 'number') {
    obj.paddingRight = style.paddingRight / window.remRadio + 'rem';
  }

  if (style.backgroundType === 1 && style.backgroundImage) {
    obj.background = "url(".concat(style.backgroundImage, ") no-repeat");
    obj.backgroundRepeat = style.componentBgRepeat ? 'repeat' : 'no-repeat';
    obj.backgroundSize = '100%';
    obj.backgroundPosition = '50% 50%';
  } else if (style.backgroundType === 2 && style.backgroundColor) {
    obj.background = style.backgroundColor;
  }

  return obj;
});