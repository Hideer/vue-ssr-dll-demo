"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var BaseLock = /** @class */ (function () {
    function BaseLock() {
        this.taskQueue = [];
    }
    BaseLock.prototype.insertTask = function (promiseCallback, index) {
        if (index > -1 && index < this.getTaskLength()) {
            this.taskQueue.splice(index, 0, promiseCallback);
        }
        else {
            this.pushTask(promiseCallback);
        }
    };
    BaseLock.prototype.pushTask = function (promiseCallback) {
        this.taskQueue.push(promiseCallback);
    };
    BaseLock.prototype.shiftTask = function () {
        return this.taskQueue.shift();
    };
    BaseLock.prototype.getTaskLength = function () {
        return this.taskQueue.length;
    };
    return BaseLock;
}());
exports.default = BaseLock;
