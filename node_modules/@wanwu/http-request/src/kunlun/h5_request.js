import httpClient from '../httpClient';
import option from './option';

const httpClientInstance = new httpClient(option);

export const initSource = httpClientInstance.initSource;
export const cancel = httpClientInstance.cancel;
export const ver = JSON.stringify(window.ENCRYPT_KEY || {
    none: 111,
});
const httpBaseInstance = httpClientInstance.httpBaseInstance;
export {
    httpBaseInstance as kl_request_base,
};
export default httpClientInstance;
