'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function getPageScroll() {
  var xScroll, yScroll;

  if (document.documentElement && document.documentElement.scrollTop) {
    // Explorer 6 Strict
    yScroll = document.documentElement.scrollTop;
    xScroll = document.documentElement.scrollLeft;
  } else if (document.body) {
    // all other Explorers
    yScroll = document.body.scrollTop;
    xScroll = document.body.scrollLeft;
  }

  var ObjPageScroll = {
    x: xScroll,
    y: yScroll
  };
  return ObjPageScroll;
}
function setPageScroll(x, y) {
  var xScroll, yScroll;

  if (document.documentElement && document.documentElement.scrollTop) {
    // Explorer 6 Strict
    yScroll = document.documentElement.scrollTop = y;
    xScroll = document.documentElement.scrollLeft = x;
  } else if (document.body) {
    // all other Explorers
    yScroll = document.body.scrollTop = y;
    xScroll = document.body.scrollLeft = y;
  }

  var ObjPageScroll = {
    x: xScroll,
    y: yScroll
  };
  return ObjPageScroll;
}

exports.getPageScroll = getPageScroll;
exports.setPageScroll = setPageScroll;
