/**
 *  数字转rem
 * @param {Number} value
 */
const filters = (value) => {
    if (value) {
        return value / 100 + 'rem';
    } else {
        return undefined;
    }
};
filters.install = Vue => {
    Vue.filter('numToRem', filters);
};
export default filters;
