'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

require('core-js/modules/es.array.for-each');
require('core-js/modules/es.object.keys');
require('core-js/modules/web.dom-collections.for-each');
var baseFn = require('@wanwu/base-fn');

function getCompDefaultOptions(compObj) {
  var Obj = {};
  var props = baseFn.fn.getObjDeepValue(compObj, 'props');
  props && Object.keys(props).forEach(function (prop) {
    var prorDefault = baseFn.fn.getObjDeepValue(props, prop + '.default');

    if (baseFn.fn.typeYalidator.isFunction(prorDefault)) {
      Obj[prop] = prorDefault();
    } else {
      Obj[prop] = prorDefault;
    }
  });
  return Obj;
}

exports.default = getCompDefaultOptions;
