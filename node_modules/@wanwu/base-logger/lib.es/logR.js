import logE from './logE';
// 预警数据上报
export default function logR(key, value = 1, logData = {}, tagArray = []) {
    logE(50001, {
        key,
        value,
        logData,
        tagArray,
    });
    logE('appBusiness', {
        key,
        value,
        logData,
        tagArray,
    });
}
