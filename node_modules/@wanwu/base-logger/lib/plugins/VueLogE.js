"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var logE_1 = require("../logE");
exports.default = (function (Vue) {
    Vue.directive('logE', {
        bind: function (el, binding) {
            el.addEventListener('click', function () {
                // const data = binding.value;
                // const eventId = parseInt(data.eventId, 10);
                // logE(eventId, data.ext);
                var data = binding.value;
                if (Array.isArray(data)) {
                    logE_1.default.apply(null, data);
                }
                else {
                    var eventId = parseInt(data.eventId, 10);
                    var event_1 = data.event;
                    logE_1.default(event_1 || eventId, data.ext);
                }
            });
        },
    });
});
