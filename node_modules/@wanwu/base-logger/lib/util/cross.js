"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var crossUtil = {
    windowName: "{}",
    initWindowName: function () {
        window.name = this.windowName;
    },
    getWindowName: function () {
        return window.name || this.windowName;
    },
    // 可能是string 或者 {}
    getWindowNameItem: function (name) {
        var currentObj = JSON.parse(this.getWindowName());
        return currentObj[name];
    },
    setWindowName: function (editObj) {
        try {
            var currentObj = JSON.parse(this.getWindowName());
            if (Object.prototype.toString.call(currentObj) !== "[object Object]") {
                currentObj = JSON.parse(this.windowName);
            }
            if (Object.prototype.toString.call(editObj) !== "[object Object]") {
                editObj = {};
            }
            var windowNameObj = __assign(__assign({}, currentObj), editObj);
            window.name = JSON.stringify(windowNameObj);
        }
        catch (err) {
            console.log(err);
        }
    },
};
exports.default = crossUtil;
