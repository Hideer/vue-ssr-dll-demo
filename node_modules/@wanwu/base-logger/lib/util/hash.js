"use strict";
// hash.js 此文件提供日志项目hash工作
Object.defineProperty(exports, "__esModule", { value: true });
var hash = {
    arr: 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789',
    // 返回长度为length+1的随机字符最后1位校验位
    rand: function (length) {
        var i;
        var str = '';
        var count = 0;
        var code;
        for (i = 0; i < length; i++) {
            code = parseInt(String(Math.random() * this.arr.length), 10);
            str += this.arr.charAt(code);
            count += this.arr.charCodeAt(code);
        }
        str += this.arr.charAt(count % this.arr.length);
        return str + Date.now().toString();
    },
    // 返回长度为length+1的随机字符最后1位校验位
    getHash: function (length) {
        var i;
        var str = '';
        var count = 0;
        var code;
        for (i = 0; i < length; i++) {
            code = parseInt(String(Math.random() * this.arr.length), 10);
            str += this.arr.charAt(code);
        }
        return new Date().getTime() + '$' + str;
    },
};
exports.default = hash;
