import { ua } from '@wanwu/base-fn';

/**
 * 判断是否进行版本控制的操作
 * （现期版本内排除wwdz_b2b，也就是收藏集市）
 * @returns {boolean} true - 进行，false - 不进行
 */
export function isToControlVersion() {
    return !['wwdz_b2b'].includes(ua.getAppName());
}
