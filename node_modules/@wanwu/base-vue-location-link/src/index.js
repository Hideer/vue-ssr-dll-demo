import comp from './index.vue';
import wLocation, {
    throughParams,
} from '@wanwu/w-location';
comp.install = (Vue, options) => {
    Vue.use(wLocation, options);
    Vue.component(comp.name, comp);
};
export default comp;
export {
    wLocation,
    throughParams,
};
